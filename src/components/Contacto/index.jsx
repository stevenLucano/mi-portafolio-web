import FormContacto from "../FormContacto";
import Mapa from "../Mapa";
import "./styles.scss";

const Contacto = () => {
  return (
    <section id="contacto">
      <FormContacto />
      <Mapa />
    </section>
  );
};

export default Contacto;
